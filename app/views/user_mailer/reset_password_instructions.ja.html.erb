<p><%= @resource.email %>さん、こんにちは！</p>

<p>gensokyo.cloudアカウントのパスワードの変更がリクエストされました。以下のリンクをクリックして手続きをお願いします。</p>

<p><%= link_to 'パスワードを変更', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>もしこのメールに身に覚えのない場合は無視してください。</p>
<p>上記のリンクにアクセスし、変更をしない限りパスワードは変更されません。</p>
