<%= @resource.email %>さん、こんにちは！

gensokyo.cloudでのパスワードの変更がリクエストされました。以下のリンクをクリックして手続きをお願いします。

<%= edit_password_url(@resource, reset_password_token: @token) %>

もしこのメールに身に覚えのない場合は無視してください。
上記のリンクにアクセスし、変更をしない限りパスワードは変更されません。
